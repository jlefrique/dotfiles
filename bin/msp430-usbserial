#!/bin/bash

if [[ "$(whoami)" != "root" ]]; then
    echo "Run this as root."
    exit 1
fi

cat >/etc/modprobe.d/usbserial.conf <<EOF
# load serial module with MSP-EXP430F5438 support
options usbserial vendor=0x0451 product=0xF500
# end
EOF
