#!/bin/bash

if [[ "$(whoami)" != "root" ]]; then
    echo "Run this as root."
    exit 1
fi

cat >/etc/modprobe.d/usbserial.conf <<EOF
# load serial module with Renesas support
options usbserial vendor=0x045b product=0x2014
# end
EOF
